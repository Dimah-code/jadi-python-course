## Broken String Exercise

Write code that first converts the following string to lower case and then outputs the string as a list of separated words using the string method.

```python
  x = 'Hi this is a string'
```